# 核心依赖
# 必须锁在 1.0 以下：1.0.0 移除了 ToolAwareSimpleAgent / note_tool / search_tool
hello-agents>=0.2.9,<1.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 搜索后端
tavily-python>=0.5.0
ddgs>=9.6.1
google-search-results>=2.4.2

# LLM 与 AI 服务
openai>=1.12.0
huggingface-hub>=1.3.3

# 音频处理
pydub>=0.25.1
# Python 3.13 起标准库移除了 audioop，pydub 需要此回填包（3.12 及以下可忽略）
audioop-lts>=0.2.2; python_version >= "3.13"

# 工具与基础库
python-dotenv==1.0.1
requests>=2.31.0
loguru>=0.7.3
